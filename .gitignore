# generated audio — drill tracks and stories ARE committed (regeneratable but
# offline-portable). Caches and eval artifacts stay ignored.
audio/.cache/
audio/eval/

# generated builds
dist/

# Piper voice models
build/.piper-voices/

# Python
build/__pycache__/
build/**/__pycache__/
build/.pytest_cache/
.venv/

# secrets
.env

# project-local trash for safe deletes (use `trash` CLI or move-to-.trash/ instead of rm -rf)
.trash/
audio/.media/
