# Audio: TTS workspace (per-fragment cache + Anki media) stays ignored;
# review-set MP3s and committed story audio under audio/ are tracked.
audio/.cache/
audio/.media/

# generated builds
dist/

# Piper voice models
build/.piper-voices/

# Python
build/__pycache__/
build/**/__pycache__/
build/.pytest_cache/
.venv/

# secrets
.env

# project-local trash for safe deletes (use `trash` CLI or move-to-.trash/ instead of rm -rf)
.trash/
